This is for Fedora 18 on OLPC.
to build on an OLPC:

./setup-olpc.sh build

Then copy the dist folder to target OLPC(s) DESKTOP and double click "Install Ustad Mobile"
The dist folder will be generated in setup/olpc/build/dist post build command above.
To run on an OLPC

./setup-olpc.sh run
